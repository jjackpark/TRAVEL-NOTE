const e=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export const venueSummary=s=>s.business?`<span class="venue-hours"><span>영업 ${e(s.business.hours)}</span><span class="venue-closed">쉬는 요일 · ${e(s.business.closed_days)}</span></span>`:'';
export const venueDetails=s=>s.business?`<dt>영업시간</dt><dd>${e(s.business.hours)}</dd><dt>쉬는 요일</dt><dd>${e(s.business.closed_days)}</dd><dt>브레이크타임</dt><dd>${e(s.business.break_time)}</dd><dt>주문 마감</dt><dd>${e(s.business.last_order)}</dd><dt>주소</dt><dd>${e(s.business.address)}</dd><dt>전화</dt><dd>${s.business.phone?`<a href="tel:${e(s.business.phone.replace(/[^+\d]/g,''))}">${e(s.business.phone)}</a>`:'공개 정보 없음'}</dd><dt>정보 확인</dt><dd>${e(s.business.checked_at)} · 공개 매장정보 기준. 임시휴무·명절 운영은 방문 전 확인해 주세요.</dd>`:`<dt>영업 안내</dt><dd>${e(s.detail.hours)}</dd>`;
export const venueList=c=>{const stops=[...new Map(c.days.flatMap(d=>d.steps).filter(s=>s.business).map(s=>[s.business.source,s])).values()];return stops.length?`<section class="venue-directory"><span class="eyebrow">RESTAURANTS & CAFES</span><h2>식당·카페 영업 안내</h2><p>금요일 출발 기준으로 배치했어요. 다른 요일에 떠난다면 쉬는 요일을 먼저 확인해 주세요. ‘휴무 없음’은 확인한 공개 주간표 기준이며 임시휴무를 보장하지 않아요.</p><div class="venue-table-wrap"><table class="venue-table"><thead><tr><th scope="col">매장</th><th scope="col">영업시간</th><th scope="col">쉬는 요일</th></tr></thead><tbody>${stops.map(s=>`<tr><th scope="row"><a href="${e(s.business.source)}" target="_blank" rel="noopener noreferrer">${e(s.name)} ↗</a></th><td>${e(s.business.hours)}</td><td>${e(s.business.closed_days)}</td></tr>`).join('')}</tbody></table></div></section>`:'';};
